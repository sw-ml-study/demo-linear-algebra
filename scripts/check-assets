#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
generated="$repo_root/out/LA01-vector-laws.svg"
committed="$repo_root/assets/previews/LA01-vector-laws.svg"
[ -f "$generated" ] || { echo "missing generated LA01 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA01 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA01 preview is stale; regenerate it from the demo" >&2; exit 1; }
generated="$repo_root/out/LA02-dot-alignment.svg"
committed="$repo_root/assets/previews/LA02-dot-alignment.svg"
[ -f "$generated" ] || { echo "missing generated LA02 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA02 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA02 preview is stale; regenerate it from the demo" >&2; exit 1; }
generated="$repo_root/out/LA03-norm-distance.svg"
committed="$repo_root/assets/previews/LA03-norm-distance.svg"
[ -f "$generated" ] || { echo "missing generated LA03 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA03 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA03 preview is stale; regenerate it from the demo" >&2; exit 1; }
generated="$repo_root/out/LA04-cosine-retrieval.svg"
committed="$repo_root/assets/previews/LA04-cosine-retrieval.svg"
[ -f "$generated" ] || { echo "missing generated LA04 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA04 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA04 preview is stale; regenerate it from the demo" >&2; exit 1; }
generated="$repo_root/out/LA05-span.svg"
committed="$repo_root/assets/previews/LA05-span.svg"
[ -f "$generated" ] || { echo "missing generated LA05 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA05 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA05 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA06-projection.svg"
committed="$repo_root/assets/previews/LA06-projection.svg"
[ -f "$generated" ] || { echo "missing generated LA06 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA06 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA06 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA07-matrix-basis.svg"
committed="$repo_root/assets/previews/LA07-matrix-basis.svg"
[ -f "$generated" ] || { echo "missing generated LA07 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA07 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA07 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA08-row-column.svg"
committed="$repo_root/assets/previews/LA08-row-column.svg"
[ -f "$generated" ] || { echo "missing generated LA08 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA08 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA08 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA09-composition.svg"
committed="$repo_root/assets/previews/LA09-composition.svg"
[ -f "$generated" ] || { echo "missing generated LA09 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA09 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA09 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA10-signed-area.svg"
committed="$repo_root/assets/previews/LA10-signed-area.svg"
[ -f "$generated" ] || { echo "missing generated LA10 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA10 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA10 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA11-linear-systems.svg"
committed="$repo_root/assets/previews/LA11-linear-systems.svg"
[ -f "$generated" ] || { echo "missing generated LA11 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA11 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA11 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA12-elimination.svg"
committed="$repo_root/assets/previews/LA12-elimination.svg"
[ -f "$generated" ] || { echo "missing generated LA12 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA12 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA12 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA13-conditioning.svg"
committed="$repo_root/assets/previews/LA13-conditioning.svg"
[ -f "$generated" ] || { echo "missing generated LA13 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA13 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA13 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA14-gram-schmidt.svg"
committed="$repo_root/assets/previews/LA14-gram-schmidt.svg"
[ -f "$generated" ] || { echo "missing generated LA14 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA14 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA14 preview is stale" >&2; exit 1; }
generated="$repo_root/out/LA15-power-iteration.svg"
committed="$repo_root/assets/previews/LA15-power-iteration.svg"
[ -f "$generated" ] || { echo "missing generated LA15 preview; run demos first" >&2; exit 1; }
[ -f "$committed" ] || { echo "missing committed LA15 preview" >&2; exit 1; }
cmp "$generated" "$committed" || { echo "LA15 preview is stale" >&2; exit 1; }
echo "assets: fresh"
