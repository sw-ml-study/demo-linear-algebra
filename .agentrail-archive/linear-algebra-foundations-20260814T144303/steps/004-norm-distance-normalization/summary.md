Implemented runnable LA03 norm, distance, and normalization curriculum. Added a shared inclusive absolute-tolerance helper and pure vector norm/distance/normalization functions with explicit Err(zero_norm) policy. Added five LA03 mlplunit cases for exactly representable inclusive tolerance boundaries plus a just-outside rejection, exact 3-4-5 norm/distance, approximate unit normalization, absolute scaling, a triangle fixture, empty/zero behavior, and shape errors; 18 repository tests pass. Added self-checking CLI and standalone web lessons, an inspected static triangle/distance/unit-direction SVG, stable README preview, freshness gate, and catalog/syllabus updates. No new blocker found; B1 rank-3 batched matmul remains the only verified blocker.