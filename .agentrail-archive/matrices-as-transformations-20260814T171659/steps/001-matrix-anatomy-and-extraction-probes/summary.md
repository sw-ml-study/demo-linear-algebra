Established and tested the rank-two matrix contract for sw-mlpl v0.20.0. Added row, column, and cell extraction helpers with structured validation; documented label, scalar, empty-dimension, bounds, and matmul mismatch behavior; expanded the capability catalog to 10 probes; and passed the complete pre-commit gate with 31 mlplunit tests. No new blocker: native out-of-bounds take diagnostics remain awkward friction, while existing B1 batched matmul remains open.